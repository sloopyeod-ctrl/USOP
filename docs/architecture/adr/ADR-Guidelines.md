A simple rule:

If a decision affects more than one engine, more than one deployment, or cannot easily be reversed, it deserves an ADR.

Examples:

Switching databases
Choosing an event bus
Authentication strategy
Multi-tenancy model
Plugin architecture

But not:

Naming a CSS class
Adding an endpoint
Refactoring a repository

This keeps the ADRs meaningful instead of turning them into a dumping ground.
