import { Typography } from "@mui/material";

export default function IdentityIntelligence() {
    return (
        <>
            <Typography variant="h4">
                Identity Intelligence
            </Typography>

            <Typography>
                This page will display the full identity graph,
                exposure score, timeline, and recommendations.
            </Typography>
        </>
    );
}